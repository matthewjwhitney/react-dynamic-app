import React from "react";
import { CardActions, Button } from "@material-ui/core";

const Actions = () => {
  return (
    <CardActions>
      <Button>Test</Button>
    </CardActions>
  );
};

export default Actions;
